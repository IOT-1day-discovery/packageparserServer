{
  "name": "renameproj",
  "version": "0.0.1",
  "description": "parses file path and extracts the binary name, then generates new json",
  "main": "rename.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Farzon Lotfi",
  "license": "ISC",
  "dependencies": {
    "JSONStream": "^1.3.5"
  }
}
